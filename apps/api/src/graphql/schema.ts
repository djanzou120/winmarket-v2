// Configuration GraphQL utilisant la nouvelle structure par domaines
import { allTypeDefs } from '../domains';

// Export des typeDefs depuis la nouvelle structure
export const typeDefs = allTypeDefs;